// eslint-disable-next-line no-unused-vars
import React from 'react';

const Characters = () => {
    return (
        <div>
            characters
        </div>
    );
};

export default Characters;